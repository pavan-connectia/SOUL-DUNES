export type Login = {
    email: string;
    password: string;
}

export type ProductType = {
    name: string;
    description: string;
    group: string;
}

export type ProductGroup = {
    name: string;
    description: string;
}